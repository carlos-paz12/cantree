//! ## tree module
//!
//! This module defines tree-related data structures. It includes
//! implementations for binary search trees and tree nodes.

/// Defines the `BinarySearchTree` struct and its associated methods,
/// including insertion, search, and removal operations.
pub mod bst;

/// Defines the `Node` struct, which represents individual elements
/// in a binary tree and stores ordered pairs with unique keys.
pub mod node;
